import { connect } from 'react-redux';

export const ms2p = (state, props) => {

};

export const md2p = (dispatch, props) => ({
    handleAuthClick,
    handleSignoutClick,
    googleAuthorize,
});

export default connect(ms2p, md2p);
